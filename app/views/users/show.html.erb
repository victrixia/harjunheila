<p id="notice"><%= notice %></p>

<table>
<tr>
  <td><strong>Käyttäjänimi:</strong></td>
  <td><%= @user.username %></td>
</tr>
  <tr>
    <td><strong>Oikea nimi:</strong></td>
    <td><%= @user.realname %></td>
  </tr>
  <tr>
    <td><strong>Onko admin:</strong></td>
    <td><%= @user.admin %></td>
  </tr>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
